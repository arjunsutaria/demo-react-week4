{
  "name": "sentry-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.js --bundle --sourcemap --outdir=dist --minify"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "dependencies": {
    "@sentry/browser": "^10.50.0"
  }
}
